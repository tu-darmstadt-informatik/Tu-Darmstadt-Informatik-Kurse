#include <stdio.h>
#include <stdlib.h>
#include <omp.h>

int main( int argc, char** argv )
{
    if( argc < 2 )
    {
        printf( "Usage: %s <array length>\n", argv[0] );
        return 1;
    }


    // Verify sorted order


    return 0;
}
